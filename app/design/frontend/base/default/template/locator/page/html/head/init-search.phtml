<script type="text/javascript">
    document.observe("dom:loaded", function() {
        var locations = '<?php echo $this->getLocations()->toJson(); ?>';

        options = {
            //fixedMap:0;
        }
        var search = new Locator.Search(options).initState(locations);

        <?php if(Mage::getStoreConfig("locator_settings/search/use_places_autocomplete")){
            //initializeAutocomplete();
        }; ?>
        <!--    Locator.settings.country_code = "--><?php //echo Mage::getStoreConfig("locator_settings/search/country_code"); ?><!--";-->
    });
</script>
